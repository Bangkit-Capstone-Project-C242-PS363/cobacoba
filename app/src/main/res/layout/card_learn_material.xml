<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:card_view="http://schemas.android.com/apk/res-auto"
    android:layout_gravity="center"
    card_view:cardBackgroundColor="@color/white"
    android:layout_marginBottom="12dp"
    android:layout_marginStart="27dp"
    android:layout_marginEnd="27dp"
    card_view:cardCornerRadius="8dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_marginStart="12dp"
        android:layout_marginEnd="12dp"
        >

        <TextView
            android:id="@+id/tvMaterialTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:fontFamily="@font/poppins_bold"
            android:textSize="16sp"
            android:text="@string/material_title"
            android:textColor="@color/dark_blue"
            card_view:layout_constraintBottom_toBottomOf="parent"
            card_view:layout_constraintStart_toEndOf="@+id/circleStroke"
            card_view:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/circleStroke"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:src="@drawable/circle_stroke"
            card_view:layout_constraintBottom_toBottomOf="parent"
            card_view:layout_constraintStart_toStartOf="parent"
            card_view:layout_constraintTop_toTopOf="parent" />

        <ImageView
            android:id="@+id/iconStudyCard"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:src="@drawable/introduction_icon"
            card_view:layout_constraintBottom_toBottomOf="@+id/circleStroke"
            card_view:layout_constraintEnd_toEndOf="@+id/circleStroke"
            card_view:layout_constraintStart_toStartOf="@+id/circleStroke"
            card_view:layout_constraintTop_toTopOf="@+id/circleStroke" />

        <!-- Lock Icon (Hidden by Default) -->
        <ImageView
            android:id="@+id/iconLock"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginEnd="8dp"
            android:layout_marginTop="8dp"
            android:src="@drawable/ic_lock"
            android:contentDescription="@string/locked_icon_description"
            android:visibility="gone"
            card_view:layout_constraintTop_toTopOf="parent"
            card_view:layout_constraintEnd_toEndOf="parent"/>

        <ImageView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:src="@drawable/baseline_navigate_next"
            card_view:layout_constraintBottom_toBottomOf="parent"
            card_view:layout_constraintEnd_toEndOf="parent"
            card_view:layout_constraintTop_toTopOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>